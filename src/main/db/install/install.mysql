CREATE TABLE MFA_ACCOUNTS (
     ID INT UNSIGNED NOT NULL AUTO_INCREMENT,
     ACCOUNT_NAME VARCHAR(50) NOT NULL,
     USERPASSWORD VARCHAR(100) NOT NULL,
     PRIMARY KEY(ID, ACCOUNT, USERPASSWORD)
);